# Project Rules
- Always verify the correct source/module directory before running compile commands.
- For scout model integration changes, increment the 3rd version digit each time (e.g., scout-core1.0.1 → scout-core1.0.2 ...) until explicitly told to use scout-core1.1.1.
